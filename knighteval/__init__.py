"""KnightEval — pytest for RAG.

Minimal-config RAG evaluation: point it at any HTTP RAG endpoint, give it a
JSONL dataset, get a terminal scorecard, a self-contained HTML report, JSON
results, and CI-stable exit codes.

This is a pre-alpha placeholder release that reserves the name on PyPI. The CLI
and evaluation engine are being built in public — see
https://github.com/GabbarX/knighteval and the project ROADMAP.
"""

__version__ = "0.0.1"
